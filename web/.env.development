# Development Environment Variables
# Used when running Vite in development mode (npm run dev)

# API Base URL for development
# Use Vite proxy to avoid CORS issues (recommended)
# The proxy is configured in vite.config.ts to forward /api -> http://localhost:8000
VITE_API_BASE_URL=/api

# Alternative: Direct connection (may have timing issues)
# VITE_API_BASE_URL=http://localhost:8000
