# Production Environment Variables
# Used when building for production (npm run build)

# API Base URL for production
# Leave empty to auto-detect from window.location (recommended)
# Or set to production API URL if using separate domain
# VITE_API_BASE_URL=https://api.lilydev.cloud

# For same-domain deployment (Nginx proxy), leave this empty
VITE_API_BASE_URL=
